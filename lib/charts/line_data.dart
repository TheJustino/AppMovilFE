/*import 'package:app_movil/charts/individual_line.dart';

class line_data {
  final double JunAmount;
  final double FebrAmount;
  final double MarchAmount;
  final double AprilAmount;
  final double MayAmount;
  final double JuneAmount;
  final double JulyAmount;

  line_data({
    required this.JunAmount,
    required this.FebrAmount,
    required this.MarchAmount,
    required this.AprilAmount,
    required this.MayAmount,
    required this.JuneAmount,
    required this.JulyAmount,
  });

  List<line_data> lineData = [];

  //iniciar datos de la barra
  void initializeLine_data(){
    lineData = [
      line_chart(x: 0, y: JunAmount),
      line_chart(x: 1, y: FebrAmount),
      line_chart(x: 2, y: MarchAmount),
      line_chart(x: 3, y: AprilAmount),
      line_chart(x: 4, y: MayAmount),
      line_chart(x: 5, y: JuneAmount),
      line_chart(x: 6, y: JulyAmount),
    ];
  }
}
*/